# Copy to .env and fill in values. Do NOT commit .env.
# For local development: export these variables before running the app.

# MongoDB Atlas URI (required)
SPRING_DATA_MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/retail_product?retryWrites=true&w=majority

# Target Products API key
API_TARGET_KEY_VALUE=your_target_api_key
